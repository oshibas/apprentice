```mermaid
graph TD;

subgraph "受注処理（今回はしない）"
  A[注文受付] --> B[在庫確認]
  B --> |在庫あり| C[発送準備]
  B --> |在庫不足| D[発注依頼]
  D --> E[発注確認]
  E --> F[入荷処理]
  F --> C
end

subgraph "商品管理"
  G[商品登録] --> |商品情報入力| H[商品マスタ登録]
  G --> |在庫情報入力| I[在庫マスタ登録]
  G --> |画像アップロード| J[画像管理]
  G --> |カテゴリ設定| K[カテゴリマスタ登録]
  G --> |価格設定| L[価格マスタ登録]
end

subgraph "顧客管理（今回はしない）"
  M[顧客登録] --> |顧客情報入力| N[顧客マスタ登録]
  M --> |住所入力| O[住所マスタ登録]
end

subgraph "マーケティング"
  P[広告戦略立案] --> Q[広告媒体選定]
  Q --> R[広告制作]
  R --> S[広告配信]
end

subgraph "決済処理（今回はしない）"
  T[注文決済] --> U[決済処理]
  U --> V[決済完了通知]
  V --> W[受注処理]
end

subgraph "発送・配送（今回はしない）"
  X[発送指示] --> Y[出荷処理]
  Y --> Z[配送手配]
  Z --> AA[配送実施]
end

A --> G
A --> M
A --> P
A --> T
C --> X
```

```mermaid
graph TD;

subgraph "トップページ"
  A[ヒーローエリア表示] --> B[新着商品表示]
  A --> C[お知らせ管理]
end

subgraph "商品関連ページ"
  D[商品一覧表示] --> |カテゴリ表示| E[カテゴリ別絞り込み]
  D --> |絞り込み検索| F[検索結果表示]
  D --> |ページング| G[ページ切り替え]
  E --> F
  F --> G
  G --> |商品詳細表示| H[商品詳細ページ]
  H --> |購入ボタン表示| I[カートページへ移動]
  H --> |その他| J[特定商取引、利用規約、よくある質問、お問い合わせ]
end

subgraph "ユーザーページ"
  K[ログインページ] --> L[ID・パスワード発行]
  L --> M[マイページ]
  M --> |注文履歴表示| N[注文履歴ページ]
  M --> |お気に入り表示| O[お気に入り商品ページ]
end

subgraph "クライアントの管理ページ"
  P[商品管理画面] --> |商品情報追加/編集/削除| Q[商品情報管理]
end

subgraph "多言語対応"
  R[言語切替] --> S[英語表示]
  R --> T[中国語表示]
end

A --> D
D --> H
H --> I
H --> J
K --> L
L --> M
M --> N
M --> O
P --> Q
R --> S
R --> T
```

````mermaid
graph TD;

 "トップページ"
  A[ヒーローエリア表示] --> B[新着商品表示]
  A --> C[お知らせ管理]
end

subgraph "商品関連ページ"
  D[商品一覧表示] --> |カテゴリ表示| E[カテゴリ別絞り込み]
  D --> |絞り込み検索| F[検索結果表示]
  D --> |ページング| G[ページ切り替え]
  E --> F
  F --> G
  G --> |商品詳細表示| H[商品詳細ページ]
  H --> |購入ボタン表示| I[カートページへ移動]
  H --> |その他| J[特定商取引、利用規約、よくある質問、お問い合わせ]
end

subgraph "ユーザーページ"
  K[ログインページ] --> L[ID・パスワード発行]
  L --> M[マイページ]
  M --> |注文履歴表示| N[注文履歴ページ]
  M --> |お気に入り表示| O[お気に入り商品ページ]
end

subgraph "クライアントの管理ページ"
  P[商品管理画面] --> |商品情報追加/編集/削除| Q[商品情報管理]
end

subgraph "多言語対応"
  R[言語切替] --> S[英語表示]
  R --> T[中国語表示]
end

A --> D
D --> H
H --> I
H --> J
K --> L
L --> M
M --> N
M --> O
P --> Q
R --> S
R --> T
```
